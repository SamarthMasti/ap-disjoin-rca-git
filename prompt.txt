I need you to professionally refactor and integrate my existing AP Disjoin RCA backend automation project into a scalable PySide6 GUI architecture without breaking the current working CLI workflow.

IMPORTANT:
Take your time and carefully analyze the existing project structure and logic before modifying anything. Do NOT rush changes. Preserve all existing backend behavior and workflow logic.

CURRENT SITUATION

I currently have a fully working backend automation system for AP Disjoin RCA workflows.

The backend already supports:

* MDT telemetry monitoring
* SNMP trigger support
* EEM integration
* Sliding window event detection
* RCA workflow orchestration
* Parallel multithreaded workflows
* AP-side telemetry collection
* WLC-side debugging
* EPC captures
* TFTP exports
* Finalization workflows
* JSON-based state management
* Report generation
* Event history
* Workflow tracking

The backend currently runs through:

python ap_disjoin_monitor_tool.py monitor ...

This existing CLI workflow MUST continue working after the refactor.

DO NOT BREAK THE EXISTING TOOL.

ARCHITECTURE GOAL

I want to evolve the project into a professional enterprise-style architecture where:

* the backend engine is reusable
* the GUI becomes a frontend/orchestration layer
* the CLI still works
* future workflows can be added later

I do NOT want the GUI to launch the backend as a subprocess.

DO NOT use subprocess.Popen() as the primary integration architecture.

Instead:

* the GUI should directly import and use the backend engine as Python modules
* the backend should remain frontend-agnostic
* both CLI and GUI should use the same engine

IMPORTANT DESIGN PRINCIPLE

The backend engine should NOT know whether:

* CLI launched it
* GUI launched it
* future API launched it

The backend should only know:
“I received a runtime configuration. Start monitoring.”

TARGET PROJECT STRUCTURE

Refactor toward this structure:

AP_DISJOIN_GUI/
│
├── gui/
│   ├── pages/
│   ├── widgets/
│   ├── dialogs/
│   ├── themes/
│   └── controllers/
│
├── backend/
│   ├── engine/
│   ├── adapters/
│   ├── telemetry/
│   ├── snmp/
│   ├── rca/
│   ├── state/
│   └── reports/
│
├── inventory/
├── logs/
├── captures/
├── reports/
└── state/

IMPORTANT:
Do NOT aggressively split files immediately.

Refactor gradually and safely.

FIRST GOAL

Keep the current:
ap_disjoin_monitor_tool.py

fully functional.

It should still support:

python ap_disjoin_monitor_tool.py monitor --device WLC_1

after the refactor.

The CLI wrapper should eventually become lightweight and simply call the backend engine.

BACKEND REFACTOR GOALS

Gradually move reusable backend logic into dedicated modules.

Examples:

backend/engine/

* monitor_engine.py
* workflow_manager.py
* event_engine.py
* finalizer.py

backend/telemetry/

* grpc_receiver.py
* eem_parser.py
* telemetry_collector.py

backend/snmp/

* trap_receiver.py
* snmp_parser.py

backend/rca/

* wlc_debug.py
* ap_debug.py
* correlation_engine.py
* evidence_collector.py

backend/reports/

* report_generator.py
* export_manager.py

backend/state/

* workflow_state.py
* session_store.py
* finalized_history.py
* event_history.py

DO NOT redesign the backend workflow logic.

The following existing behavior MUST remain unchanged:

* sliding window logic
* event validation logic
* RCA workflow triggering
* active workflow tracking
* AP disjoin counters
* finalization logic
* telemetry collection
* EPC workflow
* threading behavior
* JSON state handling

GUI REQUIREMENTS

The GUI is only a frontend/orchestration layer.

The GUI should:

* collect user configuration
* build runtime configuration objects
* launch backend engine inside worker threads
* display real-time logs and workflow state

The GUI must NEVER contain:

* Netmiko logic
* telemetry collectors
* RCA business logic
* SSH orchestration
* event engine logic

Those remain inside backend modules.

INTEGRATION MODEL

Correct integration:

GUI
↓
Controller Layer
↓
Backend Adapter
↓
MonitorEngine.start(config)

NOT:

GUI
↓
subprocess.Popen("python backend.py")

THREADING REQUIREMENTS

The backend must run inside dedicated QThread workers.

The GUI thread must remain responsive.

Backend events/logs should be emitted through Qt signals.

I want a proper event bridge architecture:

Backend
↓
Adapter
↓
Qt Signals
↓
GUI Updates

This should later support:

* live logs
* active AP cards
* workflow timelines
* finalized AP tables
* event history
* telemetry status

RUNTIME CONFIGURATION

Create a clean runtime configuration model/object that both CLI and GUI can use.

The GUI should build the same runtime configuration object that the CLI builds from argparse.

Example conceptually:

* device_name
* wlc_ip
* username
* password
* grpc_port
* trigger_mode
* tftp_server
* report_dir
* snmp_enabled
* epc_enabled

Both GUI and CLI should eventually use the same backend entrypoint:

engine.start(config)

VERY IMPORTANT

Do NOT break current functionality.

Do NOT rewrite everything from scratch.

Do NOT over-engineer immediately.

Refactor incrementally and safely.

The current working backend logic is extremely important and must remain stable.

IMPLEMENTATION APPROACH

I want you to:

1. Analyze the existing codebase first
2. Identify reusable backend components
3. Create the new architecture gradually
4. Preserve current CLI compatibility
5. Add clean GUI integration points
6. Introduce modular backend structure
7. Add proper controller/adaptor layers
8. Keep all workflow behavior identical

The final result should support:

* CLI frontend
* PySide6 GUI frontend
* future REST/API frontend

all using the SAME backend engine.

Please proceed carefully and professionally.
# 1. Project Goal

I need you to professionally refactor and integrate my existing AP Disjoin RCA automation backend into a scalable PySide6 GUI architecture while preserving all current functionality.

The existing backend already works correctly and must remain stable.

The goal is to evolve the project into an enterprise-style architecture where:

* the backend engine is reusable
* the GUI becomes a frontend/orchestration layer
* the CLI still works
* future workflows can be added later
* multiple frontends can use the same backend engine

The backend should eventually support:

* CLI frontend
* PySide6 GUI frontend
* future REST/API frontend

using the SAME backend engine.

IMPORTANT:
Do NOT rewrite the project from scratch.
Refactor incrementally and safely.

Take your time and carefully analyze the current codebase before making changes.

---

# 2. Current Architecture

The current project already contains a fully working AP Disjoin RCA automation system.

Current capabilities include:

* MDT telemetry monitoring
* SNMP trigger support
* EEM integration
* Sliding window event detection
* RCA workflow orchestration
* Parallel multithreaded workflows
* AP-side telemetry collection
* WLC-side debugging
* EPC captures
* TFTP exports
* JSON-based state handling
* Event history tracking
* Workflow tracking
* Finalization workflows
* Report generation

The current tool is executed using:

python ap_disjoin_monitor_tool.py monitor ...

This current CLI workflow MUST continue working after the refactor.

DO NOT BREAK THE EXISTING TOOL.

The current backend logic already works correctly.
Preserve all workflow behavior exactly.

---

# 3. Refactor Goals

I want the backend to be modularized into reusable components.

Target architecture:

AP_DISJOIN_GUI/
│
├── gui/
│   ├── pages/
│   ├── widgets/
│   ├── dialogs/
│   ├── themes/
│   └── controllers/
│
├── backend/
│   ├── engine/
│   ├── adapters/
│   ├── telemetry/
│   ├── snmp/
│   ├── rca/
│   ├── state/
│   └── reports/
│
├── inventory/
├── logs/
├── captures/
├── reports/
└── state/

The refactor should:

* separate backend responsibilities cleanly
* isolate GUI from backend business logic
* allow GUI and CLI to share the same engine
* support future workflow additions
* preserve current behavior

Do NOT aggressively split files immediately.
Refactor gradually and safely.

The current:
ap_disjoin_monitor_tool.py

should eventually become a lightweight CLI wrapper.

---

# 4. Backend Rules

The backend must remain independent from the GUI.

The backend should NEVER know whether it was started from:

* CLI
* GUI
* future API

The backend should only know:
“I received a runtime configuration. Start monitoring.”

DO NOT move backend business logic into GUI files.

The following behavior MUST remain unchanged:

* sliding window logic
* event validation
* workflow triggering
* workflow counters
* AP disjoin tracking
* finalization logic
* telemetry collection
* EPC workflow
* threading behavior
* JSON state handling
* report generation

Gradually move reusable backend components into modules such as:

backend/engine/

* monitor_engine.py
* workflow_manager.py
* event_engine.py
* finalizer.py

backend/telemetry/

* grpc_receiver.py
* eem_parser.py
* telemetry_collector.py

backend/snmp/

* trap_receiver.py
* snmp_parser.py

backend/rca/

* wlc_debug.py
* ap_debug.py
* correlation_engine.py
* evidence_collector.py

backend/reports/

* report_generator.py
* export_manager.py

backend/state/

* workflow_state.py
* session_store.py
* finalized_history.py
* event_history.py

IMPORTANT:
Do NOT redesign backend logic.
Only modularize and improve structure safely.

---

# 5. GUI Rules

The GUI should act only as:

* orchestration layer
* visualization layer
* configuration layer

The GUI must NEVER contain:

* Netmiko logic
* telemetry collectors
* event engine logic
* RCA orchestration
* SSH logic
* backend workflow logic

The GUI should:

* collect configuration
* build runtime configuration objects
* launch backend engine
* display workflow state
* display live logs
* remain responsive

The GUI should use:

* PySide6
* Qt Widgets
* QStackedWidget navigation
* worker threads
* signal/slot architecture

The GUI should support future workflows later.

---

# 6. Threading Rules

The backend must run inside dedicated QThread workers.

The GUI thread must NEVER block.

The architecture should support:

* live monitoring
* continuous workflows
* long-running backend execution
* real-time updates

Use:

* Qt signals
* event bridge architecture
* thread-safe communication

Backend events should eventually support:

* live logs
* workflow updates
* finalized AP notifications
* telemetry status
* active workflow state

DO NOT run the backend directly in the GUI thread.

---

# 7. Integration Rules

IMPORTANT:
Do NOT use subprocess.Popen() as the primary integration architecture.

WRONG:

GUI
↓
subprocess.Popen("python backend.py")

CORRECT:

GUI
↓
Controller Layer
↓
Backend Adapter
↓
MonitorEngine.start(config)

The GUI should directly import backend modules as Python modules.

The GUI and CLI should BOTH use the SAME backend engine.

Create:

* runtime configuration model
* backend adapter layer
* controller layer

Both CLI and GUI should eventually call:

engine.start(config)

The backend engine should be reusable and frontend-agnostic.

---

# 8. Final Architecture

Final architecture should support:

CLI Flow:
ap_disjoin_monitor_tool.py
↓
MonitorEngine.start(config)

GUI Flow:
PySide6 GUI
↓
Controller
↓
MonitorEngine.start(config)

Both should use the SAME backend engine.

The backend engine should become the central reusable system.

The GUI should eventually support:

* workflow dashboards
* live logs
* active AP monitoring
* finalized AP tables
* workflow timelines
* telemetry status
* event history

without changing backend logic.

---

# 9. Important Constraints

IMPORTANT RULES:

* Do NOT break the current CLI tool
* Do NOT rewrite everything from scratch
* Do NOT redesign backend logic
* Do NOT change workflow behavior
* Do NOT remove existing functionality
* Refactor incrementally and safely
* Preserve all current workflow behavior
* Analyze the codebase carefully before changing anything

First:
analyze the existing codebase and identify reusable components safely.

Then:
gradually refactor toward the new architecture.

The current working backend is extremely important and must remain stable throughout the migration.

Take your time and implement this professionally.
